# goreleaser.yml
# Build customization
build:
  binary: gows
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

# goreleaser.yml
brew:
  # Repository to push the tap to.
  github:
    owner: dpastoor 
    name: homebrew-tap 
